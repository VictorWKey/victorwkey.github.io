---
interface Props {
	img: string;
  text: string;
  link: string;
}

const { img, text, link} = Astro.props;
---

<a 
  href = {link} 
  class="rounded-3xl border border-black flex gap-2 py-2 px-6 justify-center items-center transform transition-transform hover:scale-110"
  target="_blank"
  rel="noopener"
>
  <img class="size-6" src={img} alt="">
  <p class="text-xl">{text}</p>
</a>